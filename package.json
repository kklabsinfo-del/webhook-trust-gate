{
  "name": "webhook-trust-gate",
  "version": "1.0.0",
  "private": false,
  "description": "Verified, deduplicated, auditable webhook trust gate for GitHub Actions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "github-action",
    "webhook",
    "stripe",
    "razorpay",
    "security"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/cache": "^3.2.4",
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.3.3"
  }
}
